(in-package #:prompt-universe)

(defprompt-method fusion-evolution
  (:intent "controlled cross-theme fusion with preserve rendering-language")
  (:semantic-tags :fusion :evolution :cross-theme)
  (:inject
   (:evolution-strategy
    "merge identities through structured extraction, not keyword mixing"
    "preserve rendering-language and negative-boundary"
    "fusion exists in color, shape, rhythm, and composition"
    "graphic design fusion, not physical mutation"
    "not material melting, not liquid anatomy, not body deformation")))