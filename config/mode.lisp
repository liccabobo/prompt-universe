;;;; Mode switch — numeric mode IDs (extend with 2, 3, … as new modes ship)
;;;;
;;;; :current-mode
;;;;   0 — illustration（主模式）：illustration/seeds/、batch-generate、output/
;;;;   1 — reserved for rebuilt story mode

((:current-mode . 0))
